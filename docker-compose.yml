services:
  openclaw:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: openclaw
    volumes:
      - .:/app
      - ./storage:/app/storage
      - ./output:/app/output
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: npm start
